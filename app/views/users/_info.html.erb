<table class='table'>
  <tr><%= image_tag user.get_profile_image, size:'50x50'  %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  <tr>
    <th>follows</th>
    <th><%= link_to user.followings.count, class: 'user_relationships_path' %></th>
  </tr>
  <tr>
    <th>followers</th>
    <th><%= link_to user.followers.count, class: 'user_relationships_path' %></th>
  </tr>
        <% if user != current_user %>
          <% if current_user.following?(user) %>
          <td><%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, class:'btn btn-info' %></td>
         <% else%>
          <td><%= link_to "フォローする", user_relationships_path(user.id), method: :post, class: 'btn btn-success' %></td>
         <% end %>
        <% else %>
          <td></td>
        <% end %>

</table>

<div class='row'>
  <% if user == current_user %>
   <%= link_to edit_user_path(user), class: "btn btn-outline-secondary btn-block edit_user_#{user.id}" do %>
     <i class="fas fa-user-cog"></i>
   <% end %>
  <% else %>
   <div></div>
  <% end %>

</div>